For enviroment settings in this directory run

For python client

export PYTHONPATH=`pwd`
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:`pwd`

For Python use....

import CameraClientWrapper
x = CameraClientWrapper.CameraClient()
x.Connect("unix:/tmp/CameraServer")
x.Ping

x.Quit, etc...



For csharp

using System;

public class runme {
	static void Main() {
		CameraClient x = new CameraClient();
		x.Connect("unix:/tmp/CameraServer");
		x.Ping();
		x.Quit();
	}
}

 gmcs -nologo -out:runme.exe *.cs && ./runme.exe



